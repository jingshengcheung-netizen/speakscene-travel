# Optional. Leave empty to use Demo mode (scripted coach replies).
OPENAI_API_KEY=

# OpenAI-compatible Chat Completions base URL (no trailing path besides /v1).
OPENAI_BASE_URL=https://api.openai.com/v1

# Chat model name.
OPENAI_MODEL=gpt-4o-mini
